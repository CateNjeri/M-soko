
import { cartQuantity } from 'coffee-ecommerce/helpers/cart-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | cart quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartQuantity([42]);
  assert.ok(result);
});

