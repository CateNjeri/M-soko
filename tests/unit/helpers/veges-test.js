
import { veges } from 'coffee-ecommerce/helpers/veges';
import { module, test } from 'qunit';

module('Unit | Helper | veges');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = veges([42]);
  assert.ok(result);
});

